{
  "name": "@jsdom-extra/html-util",
  "version": "1.0.0",
  "description": "",
  "homepage": "https://github.com/bluelovers/ws-jsdom-extra/tree/master/packages/@jsdom-extra/html-util#readme",
  "bugs": {
    "url": "https://github.com/bluelovers/ws-jsdom-extra/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bluelovers/ws-jsdom-extra.git"
  },
  "license": "ISC",
  "author": "bluelovers",
  "main": "index.js",
  "scripts": {
    "coverage": "npx nyc yarn run test",
    "lint": "npx eslint **/*.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:jest": "jest --coverage",
    "test:mocha": "npx mocha --require ts-node/register \"!(node_modules)/**/*.{test,spec}.{ts,tsx}\"",
    "npm:publish": "npm publish",
    "npm:publish:lerna": "lerna publish --yes",
    "prepublish:lockfile": "npx sync-lockfile .",
    "prepublishOnly_": "yarn run ncu && yarn run sort-package-json && yarn run test",
    "postpublish_": "git commit -m \"chore(release): publish\" .",
    "ncu": "npx yarn-tool ncu -u",
    "sort-package-json": "npx yarn-tool sort",
    "tsc:default": "tsc -p tsconfig.json",
    "tsc:esm": "tsc -p tsconfig.esm.json"
  },
  "devDependencies": {
    "@types/html-encoding-sniffer": "^2.0.0",
    "@types/html-minifier": "^3.5.3",
    "@types/whatwg-encoding": "^1.0.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "html-encoding-sniffer": "^2.0.1",
    "html-minifier": "^4.0.0",
    "whatwg-encoding": "^1.0.5"
  }
}
